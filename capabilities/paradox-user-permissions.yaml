naftiko: 1.0.0-alpha2
info:
  label: Paradox API — User Permissions
  description: 'Paradox API — User Permissions. 3 operations. Lead operation: Paradox Get user location permissions. Self-contained
    Naftiko capability covering one Paradox business surface.'
  tags:
  - Paradox
  - User Permissions
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    PARADOX_API_KEY: PARADOX_API_KEY
capability:
  consumes:
  - type: http
    namespace: paradox-user-permissions
    baseUri: https://api.paradox.ai/api/v1/public
    description: Paradox API — User Permissions business capability. Self-contained, no shared references.
    resources:
    - name: users-oid-permissions-locations
      path: /users/{oid}/permissions/locations
      operations:
      - name: getuserlocationpermissions
        method: GET
        description: Paradox Get user location permissions
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: adduserlocationpermission
        method: PUT
        description: Paradox Add user location permission
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: deleteuserlocationpermission
        method: DELETE
        description: Paradox Delete user location permission
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: bearer
      token: '{{env.PARADOX_API_KEY}}'
  exposes:
  - type: rest
    namespace: paradox-user-permissions-rest
    port: 8080
    description: REST adapter for Paradox API — User Permissions. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/users/{oid}/permissions/locations
      name: users-oid-permissions-locations
      description: REST surface for users-oid-permissions-locations.
      operations:
      - method: GET
        name: getuserlocationpermissions
        description: Paradox Get user location permissions
        call: paradox-user-permissions.getuserlocationpermissions
        outputParameters:
        - type: object
          mapping: $.
      - method: PUT
        name: adduserlocationpermission
        description: Paradox Add user location permission
        call: paradox-user-permissions.adduserlocationpermission
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: deleteuserlocationpermission
        description: Paradox Delete user location permission
        call: paradox-user-permissions.deleteuserlocationpermission
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: paradox-user-permissions-mcp
    port: 9090
    transport: http
    description: MCP adapter for Paradox API — User Permissions. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: paradox-get-user-location-permissions
      description: Paradox Get user location permissions
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: paradox-user-permissions.getuserlocationpermissions
      outputParameters:
      - type: object
        mapping: $.
    - name: paradox-add-user-location-permission
      description: Paradox Add user location permission
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: paradox-user-permissions.adduserlocationpermission
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: paradox-delete-user-location-permission
      description: Paradox Delete user location permission
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: paradox-user-permissions.deleteuserlocationpermission
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
